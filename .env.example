# =============================================================================
# Mimo MCP Server - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# All settings are optional - Mimo works with sensible defaults.
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Configuration (OpenRouter)
# -----------------------------------------------------------------------------
# Get a free API key at: https://openrouter.ai/keys
# Without this, Mimo will have limited reasoning capabilities
OPENROUTER_API_KEY=

# Primary LLM model (default: Mistral Small 3.1 24B - free, 128K context)
# Other free options:
#   - google/gemma-3-27b-it:free
#   - google/gemini-2.0-flash-exp:free
#   - meta-llama/llama-3.3-70b-instruct:free
OPENROUTER_MODEL=mistralai/mistral-small-3.1-24b-instruct:free

# Fallback model if primary fails (default: Gemma 3 27B)
OPENROUTER_FALLBACK_MODEL=google/gemma-3-27b-it:free

# Vision model for image analysis (default: NVIDIA Nemotron)
OPENROUTER_VISION_MODEL=nvidia/nemotron-nano-12b-v2-vl:free

# -----------------------------------------------------------------------------
# Embedding Configuration (Local Ollama)
# -----------------------------------------------------------------------------
# Mimo uses local Ollama for embeddings - install from: https://ollama.ai
# Then run: ollama pull qwen3-embedding:0.6b
OLLAMA_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=qwen3-embedding:0.6b

# -----------------------------------------------------------------------------
# External Service API Keys (Optional Skills)
# -----------------------------------------------------------------------------
# GitHub integration - for repository operations
# Get token at: https://github.com/settings/tokens
GITHUB_TOKEN=

# Exa AI - for advanced web search
# Get key at: https://exa.ai
EXA_API_KEY=

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# HTTP API port
MIMO_HTTP_PORT=4000

# MCP stdio server port (internal)
MCP_PORT=9000

# Host for external access
MIMO_HOST=localhost

# API key for HTTP authentication (generate your own)
# Example: openssl rand -hex 32
MIMO_API_KEY=

# Secret key base for Phoenix (generate your own)
# Example: mix phx.gen.secret
MIMO_SECRET_KEY_BASE=

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# SQLite database path
DB_PATH=priv/mimo_mcp.db

# Connection pool size
POOL_SIZE=10

# -----------------------------------------------------------------------------
# Skills Configuration
# -----------------------------------------------------------------------------
# Path to skills definition file
SKILLS_PATH=priv/skills.json

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Log level: debug, info, warn, error, none
# Use "error" or "none" for stdio mode to keep output clean
LOGGER_LEVEL=info
